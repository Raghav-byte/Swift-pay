apiVersion: v1
kind: ConfigMap
metadata:
  name: swiftpay-config
data:
  REDIS_HOST: redis
  REDIS_PORT: "6379"
  KAFKA_BOOTSTRAP: kafka:9092
  # Override for external Supabase: set DB_URL via Secret instead of local postgres
